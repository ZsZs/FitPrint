# must be unique in a given SonarQube instance
sonar.projectKey=com.processpuzzle.fitnesse:fitnesse-connect-connect
# this is the name and version displayed in the SonarQube UI. Was mandatory prior to SonarQube 6.1.
sonar.projectName=Fit-Connect
sonar.projectVersion=1.0.10
sonar.branch=development
 
# Path is relative to the sonar-project.properties file. Replace "\" by "/" on Windows.
# Since SonarQube 4.2, this property is optional if sonar.modules is set. 
# If not set, SonarQube starts looking for source code from the directory containing 
# the sonar-project.properties file.
sonar.sources=fitnesse-connect-connect/src/main/java
 
# Encoding of the source code. Default is default system encoding
sonar.sourceEncoding=UTF-8
sonar.jacoco.reportPaths=fitnesse-connect-connect/target/coverage-reports/jacoco-ut.exec
sonar.jacoco.itReportPath=fitnesse-connect-connect/target/coverage-reports/jacoco-it.exec
sonar.junit.reportsPath=fitnesse-connect-connect/target/surefire-reports
